require: sudo
language: node_js
services:
  - docker
script: 
  docker build -t prodest/api-noticias . && docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD" && docker push prodest/api-noticias
# deploy:
#   provider: script
#   script: docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD" && docker push alvarollmenezes/mockupagenda
#   on:
#     branch: publish
